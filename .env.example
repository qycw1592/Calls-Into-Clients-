# Public frontend configuration only. Never commit real secrets.
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only values belong in Vercel environment variables and must never be committed.
